include ':app'

// To include BLE Library project as a module, clone it to Android-BLE-Library folder and
// uncomment the following lines. Also, uncomment the dependency in gradle.build file.

// include ':ble', ':ble-ktx', ':ble-livedata'
// project(':ble').projectDir = file('../Android-BLE-Library/ble')
// project(':ble-ktx').projectDir = file('../Android-BLE-Library/ble-ktx')
// project(':ble-livedata').projectDir = file('../Android-BLE-Library/ble-livedata')